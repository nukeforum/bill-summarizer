org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
org.gradle.caching=true
kotlin.code.style=official

# Version of the org.gradle.toolchains.foojay-resolver-convention settings
# plugin (see settings.gradle.kts). Keep in sync with foojayResolverVersion
# in android/gradle.properties — separate Gradle builds cannot share one
# properties file idiomatically.
foojayResolverVersion=1.0.0
